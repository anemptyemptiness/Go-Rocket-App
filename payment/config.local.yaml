grpc:
  host: "0.0.0.0"
  port: "50052"

logger:
  level: "debug"

otel:
  enable_otlp: true
  endpoint: "localhost:4317"
  service_name: "payment"
  environment: "dev"
  service_version: "v1.0.0"
  sampling_ratio: 0.5
